[project]
name = "apsal-open"
version = "0.8.0"
description = "Five-layer APSAL authoring, local DNA Registry, documented Prompt/Skill delivery, and Codex-native image generation"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]

[project]
name = "apsal-open"
version = "0.16.0"
description = "APSAL creative project library, reference analysis, Prompt/Skill delivery, lineage, and confirmed social sharing"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]

[project]
name = "apsal-open"
version = "0.7.0"
description = "Five-layer, thirteen-element APSAL authoring, local DNA Registry, semantic compiler, run lineage, and Codex plugin"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]

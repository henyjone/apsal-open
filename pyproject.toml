[project]
name = "apsal-open"
version = "0.5.0"
description = "Local-first APSAL conversational authoring, DNA Registry, semantic compiler, run lineage, and Codex plugin"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
